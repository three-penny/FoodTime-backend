# Git 提交日志

## 基本信息
- **分支名**：dev
- **提交时间**：2026-06-01 11:17:47
- **提交者**：NAPHthalene130
- **Commit Hash**：5632213

## 提交信息
feat(superadmin): 添加超级管理员修改用户密码功能

## 变更文件清单
| 文件路径 | 变更类型 | 说明 |
|---------|---------|------|
| app/routes/superadmin_routes.py | 修改 | 新增 PUT /users/<id>/password 路由 |
| app/services/superadmin_service.py | 修改 | 新增 change_user_password 方法 |

## 变更详情
1. `superadmin_routes.py` 新增 `PUT /api/v1/superadmin/users/<user_id>/password` 接口，需 superadmin 权限
2. `superadmin_service.py` 新增 `change_user_password()` 方法：
   - 校验新密码长度（6-128位）
   - 使用 `generate_password_hash` 哈希存储
   - 记录 `password_change` 审计日志

## 影响范围
- 影响模块：超级管理员控制台
- 影响接口：PUT /api/v1/superadmin/users/:id/password
- 向前兼容：是
