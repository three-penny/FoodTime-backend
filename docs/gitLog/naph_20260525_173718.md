# Git 提交日志

## 基本信息
- **分支名**：naph
- **提交时间**：2026-05-25 17:37:18
- **提交者**：naph130
- **Commit Hash**：4b25149

## 提交信息
refactor: 移除rant_repository内部冗余commit并完善提交日志规范

## 变更文件清单
| 文件路径 | 变更类型 | 说明 |
|---------|---------|------|
| app/repositories/rant_repository.py | 修改 | 移除 create_rant 内部冗余 db.session.commit() |
| rules.md | 修改 | 新增 12. Git提交日志记录规范，章节重新编号 12→16 |

## 变更详情
1. 移除 `rant_repository.py` 中 `create_rant` 方法的内部 `db.session.commit()`，统一事务提交职责到 Service 层，与 `dish_submission_repository` 保持一致模式。
2. rules.md 新增第 12 章 Git提交日志记录规范，要求每次提交在 docs/gitLog/ 创建 `<BranchName>_<Time>.md` 日志，章节 12-15 重新编号为 13-16。

## 影响范围
- 影响模块：吐槽墙创建
- 影响接口：POST /api/v1/rants
- 向前兼容：是，Service 层已存在 commit 调用，功能不受影响

## 合并冲突处理指引
- 若 `rant_repository.py` 发生冲突，注意 Service 层调用后必须有显式的 `db.session.commit()` 或由外层事务管理器统一提交。
- 若 `rules.md` 发生冲突，确保新章节编号 12-16 不与既有章节重复。
