# Git 提交日志

## 基本信息
- **分支名**：naph
- **提交时间**：2026-05-26 23:56:45
- **提交者**：naph130
- **Commit Hash**：51439b0

## 提交信息
refactor: 优化食堂展示接口性能(全表扫描→主键查询/动态排序)

## 变更文件清单
| 文件路径 | 变更类型 | 说明 |
|---------|---------|------|
| app/services/dining_display_service.py | 修改 | get_canteen_spots 动态排序、get_canteen_by_id 主键查询 |

## 变更详情
1. get_canteen_spots：移除硬编码 10 个 ID，改为 ORDER BY rating DESC 动态查询全部食堂。
2. get_canteen_by_id：从全表扫描改为 filter_by(id=xxx).first() 直接主键查询。

## 影响范围
- 影响接口：GET /api/v1/canteens/spots、GET /api/v1/canteens/:id
- 向前兼容：是，响应字段未变

## 合并冲突处理指引
- 若 dining_display_service.py 冲突，确保 get_canteen_spots 使用直接 DB 查询而非硬编码 ID 列表。
