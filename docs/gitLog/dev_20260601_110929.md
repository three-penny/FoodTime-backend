# Git 提交日志

## 基本信息
- **分支名**：dev
- **提交时间**：2026-06-01 11:09:29
- **提交者**：NAPHthalene130
- **Commit Hash**：32d28f3

## 提交信息
fix(auth): 修复用户名登录失败及昵称唯一性问题

## 变更文件清单
| 文件路径 | 变更类型 | 说明 |
|---------|---------|------|
| app/repositories/auth_repository.py | 修改 | 新增 find_by_nickname 查询方法 |
| app/services/auth_service.py | 修改 | 登录新增昵称查询；注册新增昵称唯一性校验 |

## 变更详情
1. `auth_repository.py` 新增 `find_by_nickname()` 方法，按昵称查询用户
2. `auth_service.py` 登录流程搜索顺序改为：email → account(学号) → nickname(昵称)，用户可使用三者任一登录
3. `auth_service.py` 注册流程新增昵称唯一性校验，重复昵称返回 "该昵称已被使用，请更换一个。"

## 影响范围
- 影响模块：用户认证（登录/注册）
- 影响接口：POST /api/v1/auth/login、POST /api/v1/auth/register
- 向前兼容：是，原有 email/account 登录不受影响
