# Git 提交日志

## 基本信息
- **分支名**：dev
- **提交时间**：2026-06-01 11:03:06
- **提交者**：NAPHthalene130
- **Commit Hash**：1be40c9

## 提交信息
ci: 添加 GitHub Actions CI 工作流

## 变更文件清单
| 文件路径 | 变更类型 | 说明 |
|---------|---------|------|
| .github/workflows/ci.yml | 新增 | GitHub Actions CI 配置 |

## 变更详情
1. 新增 `.github/workflows/ci.yml`，包含两个并行 job：
   - `lint`: 安装依赖后执行 `ruff check .` 代码检查 + `bandit -r app` 安全扫描
   - `test`: 安装依赖后执行 `pytest --cov=app` 单元测试（含 coverage 报告，阈值 0 暂不阻塞）
2. 触发条件：push / pull_request 到 `dev` 和 `main` 分支

## 影响范围
- 影响模块：CI/CD
- 向前兼容：是
