# Git 提交日志

## 基本信息
- **分支名**：naph
- **提交时间**：2026-05-27 00:05:55
- **提交者**：naph130
- **Commit Hash**：08eb844

## 提交信息
feat: 管理员审核通过后自动创建菜品记录

## 变更文件清单
| 文件路径 | 变更类型 | 说明 |
|---------|---------|------|
| app/services/dish_submission_service.py | 修改 | audit_submission 增加审核通过后入库菜品逻辑 |

## 变更详情
1. audit_submission 不再通过 repository 间接更新，改为直接查询并更新 submission 对象。
2. 审核通过时调用 _create_dish_from_submission：
   - 按 canteen_name 查找食堂（不匹配则跳过并记录日志）。
   - _resolve_stall_id 查找或自动创建档口。
   - 生成 dish_id（canteen_id + dish_name），检查防重复。
   - _migrate_dish_image 将投稿图片从 submission_img 复制到 dish_img 目录。
   - 创建 Dish 记录写入 dishes 表。
3. 所有操作在同一 db.session 事务内，部分失败不影响 submission 状态更新。

## 影响范围
- 影响模块：管理员审核台、菜品投稿
- 影响接口：PUT /api/v1/submissions/:id/audit
- 向前兼容：是，审核驳回流程不受影响

## 合并冲突处理指引
- 若 audit_submission 方法冲突，确保审核通过分支包含 _create_dish_from_submission 调用。
- 若 import 部分冲突，确保包含 shutil。
