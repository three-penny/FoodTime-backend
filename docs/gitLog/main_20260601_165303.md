# Git 提交日志

## 基本信息
- **分支名**：main
- **提交时间**：2026-06-01 16:53:03
- **提交者**：FoodTime Backend Team
- **Commit Hash**：93e24df

## 提交信息
fix(auth): 验证码存储由内存改为数据库，解决多进程无法共享的问题

## 变更文件清单
| 文件路径 | 变更类型 | 说明 |
|---------|---------|------|
| app/entities/models.py | 修改 | 新增 VerificationCode 模型 |
| app/utils/email_utils.py | 修改 | 移除内存 dict，改用数据库存储 |
| app/__init__.py | 修改 | 新增 verification_codes 表自动创建检测 |

## 变更详情
1. 新增 VerificationCode 模型（email 为主键，含 code、expires_at）
2. send_verification_code_email：写入 DB 后再发邮件，发送失败回滚
3. verify_code：从 DB 查询验证码，校验成功/过期后删除
4. _init_database 新增 verification_codes 表缺失检测与自动建表
5. 移除内存 dict (_code_store)、锁 (_code_store_lock) 和定时清理逻辑

## 修复问题
- 多进程环境（gunicorn -w 4）下验证码无法跨 worker 共享
- 并发场景下验证码一致性问题

## 影响范围
- 影响模块：注册验证码
- 数据库迁移：自动创建 verification_codes 表（需重启应用）
- 向前兼容：是
