# Git 提交日志

## 基本信息
- **分支名**：Startale
- **提交时间**：2026-05-27 21:30:00
- **提交者**：Startale
- **Commit Hash**：TBD

## 提交信息
feat: 默认图片兜底逻辑

## 变更文件清单
| 文件路径 | 变更类型 | 说明 |
|---------|---------|------|
| config.py | 修改 | 新增 DEFAULT_IMG_FOLDER / DEFAULT_IMG_URL |
| app/services/dining_display_service.py | 修改 | image_url 为空时自动使用默认图 |
| app/services/dish_submission_service.py | 修改 | 未上传图片时使用默认图 |

## 变更详情
1. config 新增 `DEFAULT_IMG_FOLDER` 和 `DEFAULT_IMG_URL` 配置项。
2. `dining_display_service` 中 `_canteen_to_dict`/`_stall_to_dict`/`_dish_to_dict` 增加 `_img()` 函数：image_url 为空时返回默认图 URL。
3. `dish_submission_service.create_submission` 未上传图片时 image_url 不再为空字符串，而是使用默认图 URL。

## 影响范围
- 所有返回食堂/档口/菜品/提报的接口
- 向前兼容：是
